name: flutter_ecg_monitor
description: >
  A realistic, animated ECG / heartbeat monitor widget.
  Licensed under Han Conditional Fair Source (HCFS) v1.0. 
  Free for hobbyists; paid or open-source for commercial use.
version: 1.0.0
homepage: https://github.com/jsh562/flutter_ecg_monitor
repository: https://github.com/jsh562/flutter_ecg_monitor

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # No assets needed

topics:
  - ecg
  - heartbeat
  - animation
  - canvas
  - widget
